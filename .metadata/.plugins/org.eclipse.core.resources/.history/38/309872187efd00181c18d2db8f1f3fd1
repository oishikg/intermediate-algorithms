package sorting;


public class PriorityQueue 
{
	/* instance variables: note that we only implement a pq for integers, 
	 * and a variable N to keep track of the size of the pq */
	int[] pq; 
	int N;
	
	/* constructor to instantiate the priority queue with a client provided 
	 * initial size; in addition to the initial size, we implement the pq with
	 * array resizing */
	public PriorityQueue (int inSz)
	{
		pq = new int [inSz];
	}
	
	/* instance methods */
	
	/* method to return current size of pq */
	public int size ()
	{
		return N;
	}
	private void resize(int max)
	{
		int [] temp = new int[max];
		for (int i=0; i<N ; i++)
			temp[i] = pq [i];
		a = temp;
	}
}
